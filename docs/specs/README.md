# Saved specs

Large pasted specs are persisted here by the /spec skill so they survive
usage-limit stops, compactions, and session restarts. Each file has a
Status checklist at the top. See .claude/skills/spec/.
